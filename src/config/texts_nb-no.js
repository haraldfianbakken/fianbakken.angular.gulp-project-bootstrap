'use strict';
var app = angular.module('com.fianbakken.angular.bootstrap.web.translations');

app.config(function ($translateProvider) {
        $translateProvider.translations('nb-no', {
            // translation constants goes here
        });
    }
);
